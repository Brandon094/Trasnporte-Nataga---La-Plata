<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TrasnporteNatagaLaPlata" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/primary_500</item>
        <item name="colorPrimaryContainer">@color/primary_200</item>
        <item name="colorOnPrimary">@color/text_on_primary</item>
        <item name="colorOnPrimaryContainer">@color/primary_700</item>

        <!-- Secondary colors -->
        <item name="colorSecondary">@color/secondary_500</item>
        <item name="colorSecondaryContainer">@color/secondary_200</item>
        <item name="colorOnSecondary">@color/text_on_primary</item>
        <item name="colorOnSecondaryContainer">@color/secondary_700</item>

        <!-- Tertiary colors (optional) -->
        <item name="colorTertiary">@color/seat_available</item>
        <item name="colorTertiaryContainer">@color/status_confirmed</item>
        <item name="colorOnTertiary">@color/text_on_primary</item>

        <!-- Status bar -->
        <item name="android:statusBarColor">@color/primary_700</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>
    <!-- Base application theme. -->
    <style name="Base.Theme.TrasnporteNatagaLaPlata" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
</resources>