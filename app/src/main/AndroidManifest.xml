<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔹 PERMISOS PARA FCM Y CONECTIVIDAD -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrasnporteNatagaLaPlata"
        tools:targetApi="31">

        <!-- 🔹 ACTIVIDAD PRINCIPAL - Punto de entrada de la aplicación -->
        <activity
            android:name=".activities.common.InicioDeSesion"
            android:exported="true"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 🔹 SERVICIO DE NOTIFICACIONES FCM - Maneja mensajes en primer plano -->
        <service
            android:name=".services.notifications.NotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- 🔹 ACTIVIDADES DE LA APLICACIÓN -->
        <activity
            android:name=".activities.passenger.ConfirmarReserva"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.passenger.CrearReservas"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.passenger.EditarPerfil"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.driver.EditarPerfilConductor"
            android:exported="false"
            android:label="Editar Perfil Conductor"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.driver.HistorialConductor"
            android:exported="false"
            android:label="@string/historial_reservas"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.passenger.HistorialReservas"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.driver.InicioConductor"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.passenger.InicioUsuarios"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.driver.PerfilConductor"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.passenger.PerfilUsuario"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.common.RegistroUsuarios"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <!-- 🔹 METADATOS PARA FCM -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="true" />
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="true" />

    </application>

</manifest>