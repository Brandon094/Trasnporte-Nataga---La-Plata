<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 🔹 PERMISOS PARA FCM Y CONECTIVIDAD -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" /> <!-- ✅ Para mantener el dispositivo despierto al recibir notificaciones -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" /> <!-- ✅ Android 13+ para mostrar notificaciones -->
    <uses-permission android:name="android.permission.VIBRATE" /> <!-- ✅ Para vibración en notificaciones -->
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" /> <!-- ✅ Permiso específico de FCM -->

    <!-- 🔹 PERMISOS PARA LA APLICACIÓN -->
    <permission
        android:name="com.chopcode.trasnportenataga_laplata.permission.C2D_MESSAGE"
        android:protectionLevel="signature" /> <!-- ✅ Permiso personalizado para FCM -->

    <uses-permission android:name="com.chopcode.trasnportenataga_laplata.permission.C2D_MESSAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TrasnporteNatagaLaPlata"
        tools:targetApi="31">

        <!-- 🔹 ACTIVIDAD PRINCIPAL - Punto de entrada de la aplicación -->
        <activity
            android:name=".activities.InicioDeSesion"
            android:exported="true"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 🔹 INTENT FILTER PARA NOTIFICACIONES FCM -->
            <intent-filter>
                <action android:name="OPEN_NOTIFICATION" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 🔹 SERVICIO DE NOTIFICACIONES FCM - Maneja mensajes en primer plano -->
        <service
            android:name=".services.NotificationService"
            android:exported="false"
            android:stopWithTask="false"
            android:directBootAware="true"> <!-- ✅ Funciona incluso en modo de arranque directo -->
            <intent-filter android:priority="-1">
                <action android:name="com.google.firebase.MESSAGING_EVENT" /> <!-- ✅ Escucha mensajes de FCM -->
            </intent-filter>
        </service>

        <!-- 🔹 SERVICIO DE INSTANCIA ID DE FCM - Maneja tokens y mensajes en segundo plano -->
        <service
            android:name="com.google.firebase.messaging.FirebaseMessagingService"
            android:exported="true"
            android:stopWithTask="false"
            android:directBootAware="true">
            <intent-filter android:priority="-1">
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" /> <!-- ✅ Maneja actualización de tokens -->
            </intent-filter>
        </service>

        <!-- 🔹 RECEIVER PARA NOTIFICACIONES - Maneja clics en notificaciones -->
        <receiver
            android:name=".receivers.NotificationReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" /> <!-- ✅ Recibir notificaciones después del reinicio -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" /> <!-- ✅ Después de actualizar la app -->
            </intent-filter>
        </receiver>

        <!-- 🔹 SERVICIO DE INICIO DE TRABAJOS - Para procesar notificaciones en background -->
        <service
            android:name="androidx.work.impl.background.systemalarm.SystemAlarmService"
            android:exported="false" />

        <service
            android:name="androidx.work.impl.background.systemjob.SystemJobService"
            android:exported="false"
            tools:targetApi="21" />

        <!-- 🔹 ACTIVIDADES DE LA APLICACIÓN - Ordenadas alfabéticamente -->
        <activity
            android:name=".activities.ConfirmarReserva"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.CrearReservas"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.EditarPerfil"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.EditarPerfilConductor"
            android:exported="false"
            android:label="Editar Perfil Conductor"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.HistorialConductor"
            android:exported="false"
            android:label="@string/historial_reservas"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.HistorialReservas"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.InicioConductor"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.InicioUsuarios"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.PerfilConductor"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.PerfilUsuario"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <activity
            android:name=".activities.RegistroUsuarios"
            android:exported="false"
            android:theme="@style/Theme.TrasnporteNatagaLaPlata.NoActionBar" />

        <!-- 🔹 METADATOS PARA FCM -->
        <meta-data
            android:name="firebase_messaging_auto_init_enabled"
            android:value="true" /> <!-- ✅ Inicialización automática de FCM -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="true" /> <!-- ✅ Recopilación de analytics -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="true" /> <!-- ✅ Recopilación de crashlytics -->

        <!-- 🔹 SERVICIOS DE FIREBASE (ComponentDiscovery) -->
        <service
            android:name="com.google.firebase.components.ComponentDiscoveryService"
            android:exported="false">
            <meta-data
                android:name="com.google.firebase.components:com.google.firebase.auth.FirebaseAuthRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
            <meta-data
                android:name="com.google.firebase.components:com.google.firebase.database.DatabaseRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
            <meta-data
                android:name="com.google.firebase.components:com.google.firebase.messaging.FirebaseMessagingRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" /> <!-- ✅ Registrar FCM -->
            <meta-data
                android:name="com.google.firebase.components:com.google.firebase.firestore.FirestoreRegistrar"
                android:value="com.google.firebase.components.ComponentRegistrar" />
        </service>

    </application>

</manifest>